Deploy to production on Netlify (no build step needed).

```bash
netlify deploy --prod
```

- Live site: https://afarena.netlify.app
- Auto-deploy also triggers on every `git push origin main`
- No build step — Netlify serves the repo root directly
